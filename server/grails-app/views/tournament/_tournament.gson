import org.competition.Tournament

model {
    Tournament tournament
}

json g.render(tournament, [expand: ['weight', 'federation', 'category'], excludes:['players', 'byes', 'groups', 'bracketMatches']])